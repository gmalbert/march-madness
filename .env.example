# Set your CBBD API key here
# Get your API key from: https://collegebasketballdata.com/
CBBD_API_KEY = "your_api_key_here"

# Save this file as .env in the project root
# Then run: pip install python-dotenv
# And the API key will be loaded automatically

# Optional: Odds provider API key (for live betting lines / market probabilities)
# Recommended provider: The Odds API (https://the-odds-api.com/) or any compatible provider.
# Add your API key here and save this file as `.env` in the project root.
# Example (do not commit your real key):
# ODDS_API_KEY="your_odds_api_key_here"
#
# Note: the code reads `ODDS_API_KEY` from the environment. Keep this secret.